/**
 * ZAI Provider 상수
 */

// Provider ID (빌트인 provider override)
export const ZAI_PROVIDER_ID = "zai-coding-plan";

// Rate limit 쿨다운 시간 (ms)
export const COOLDOWN_MS = 60_000;  // 60초

// 디버그 로그 prefix
export const DEBUG_PREFIX = "[ZAI]";
